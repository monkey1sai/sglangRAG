# Root env example (public-safe)
# Copy to `.env` (NOT committed) and adjust paths/keys.

# -------------------------
# Orchestrator (WS: /chat)
# -------------------------
ORCH_HOST=0.0.0.0
ORCH_PORT=9100
# Optional: protect orchestrator endpoint; browser client will pass `?api_key=...`
ORCH_API_KEY=

# -------------------------
# SGLang (LLM backend)
# -------------------------
SGLANG_BASE_URL=http://localhost:8082
SGLANG_API_KEY=__REPLACE_ME__
SGLANG_MODEL=Qwen/Qwen2.5-1.5B-Instruct

# -------------------------
# ws_gateway_tts (TTS gateway)
# -------------------------
WS_TTS_URL=ws://localhost:9000/tts
# Optional: if your ws_gateway_tts requires Bearer auth
WS_TTS_API_KEY=

# delta -> TTS buffering policy
TTS_FLUSH_MIN_CHARS=12
TTS_FLUSH_ON_PUNCT=true

# Debug only: allow client override `ws_tts_url`
ALLOW_CLIENT_TTS_URL=false

# -------------------------
# ws_gateway_tts runtime (when running gateway directly)
# -------------------------
WS_TTS_HOST=0.0.0.0
WS_TTS_PORT=9000
WS_TTS_ENGINE=dummy
# Optional: protect gateway endpoint
GATEWAY_API_KEY=

# Riva (if WS_TTS_ENGINE=riva)
RIVA_SERVER=localhost:50051

# Piper (if WS_TTS_ENGINE=piper)
PIPER_BIN=
PIPER_MODEL=
PIPER_SPEAKER_ID=
PIPER_EXTRA_ARGS=
# file (stable) | stdout (requires piper supports `--output_file -`)
PIPER_OUTPUT_MODE=file
